<DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:vs="http://schemas.microsoft.com/visualstudio/extensibility/2022/xaml"
              xmlns:styles="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0">

    <Grid>
        <Grid.Resources>
            <Style TargetType="Label" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ThemedDialogLabelStyleKey}}" />
            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.TextBoxStyleKey}}" />
            <Style TargetType="Button" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ButtonStyleKey}}" />
            <Style TargetType="ListBox" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ThemedDialogListBoxStyleKey}}" />
            <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ComboBoxStyleKey}}" />

        </Grid.Resources>

        <TabControl>
            <TabItem Header="Inspector">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="120" />
                    </Grid.RowDefinitions>

                    <!-- Connection panel -->
                    <Border Grid.Row="0" Padding="8,6" BorderBrush="{DynamicResource {x:Static styles:VsBrushes.CommandBarBorderKey}}" BorderThickness="0,0,0,1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="140" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Label Grid.Column="0" Content="Transport:" VerticalAlignment="Center" />
                                <ComboBox Grid.Column="1" ItemsSource="{Binding TransportOptions}" SelectedItem="{Binding TransportType}" VerticalAlignment="Center" Margin="4,0" />

                                <Button Grid.Column="3" Content="Connect" Command="{Binding ConnectCommand}" Margin="4,0" Padding="10,3" />
                                <Button Grid.Column="4" Content="Disconnect" Command="{Binding DisconnectCommand}" Margin="4,0" Padding="10,3" />
                            </Grid>

                            <Grid Grid.Row="1" Margin="0,4,0,0">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding TransportType}" Value="stdio">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <Label Grid.Column="0" Content="Executable:" VerticalAlignment="Center" />
                                <TextBox Grid.Column="1" Text="{Binding ExecutablePath}" VerticalAlignment="Center" Margin="4,0" />

                                <Label Grid.Column="2" Content="Arguments:" VerticalAlignment="Center" />
                                <TextBox Grid.Column="3" Text="{Binding Arguments}" VerticalAlignment="Center" Margin="4,0" />
                            </Grid>

                            <Grid Grid.Row="1" Margin="0,4,0,0">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding TransportType}" Value="http">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <Label Grid.Column="0" Content="Server URL (SSE):" VerticalAlignment="Center" />
                                <ComboBox Grid.Column="1"
                                          IsEditable="True"
                                          ItemsSource="{Binding SavedServerUrls}"
                                          Text="{Binding ServerUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          VerticalAlignment="Center"
                                          Margin="4,0" />
                            </Grid>
                        </Grid>
                    </Border>

                    <!-- Main area: tools list and response -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" BorderBrush="{DynamicResource {x:Static styles:VsBrushes.CommandBarBorderKey}}" BorderThickness="0,0,1,0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <Label Grid.Row="0" Content="Available tools:" FontWeight="Bold" />
                                <ListBox Grid.Row="1" ItemsSource="{Binding Tools}" SelectedItem="{Binding SelectedTool}" Margin="4">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE90F;"
                                                           FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                                           Foreground="{DynamicResource {x:Static styles:VsBrushes.WindowTextKey}}"
                                                           Margin="0,0,6,0"
                                                           VerticalAlignment="Center" />
                                                <TextBlock Text="{Binding Name}"
                                                           VerticalAlignment="Center" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Grid>
                        </Border>

                        <Border Grid.Column="1" Background="{DynamicResource {x:Static styles:VsBrushes.ToolWindowBackgroundKey}}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <!-- Results -->
                                <Border Grid.Row="0">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                                        <TextBox Text="{Binding ResponseText}"
                                                 IsReadOnly="True"
                                                 AcceptsReturn="True"
                                                 TextWrapping="Wrap"
                                                 VerticalScrollBarVisibility="Disabled"
                                                 HorizontalScrollBarVisibility="Disabled"
                                                 FontFamily="Consolas"
                                                 FontSize="12"
                                                 Background="Transparent"
                                                 BorderThickness="0"
                                                 Padding="4"
                                                 xml:space="preserve" />
                                    </ScrollViewer>
                                </Border>

                                <!-- Tool parameters -->
                                <Border Grid.Row="1"
                                        Background="{DynamicResource {x:Static styles:VsBrushes.ToolWindowBackgroundKey}}"
                                        BorderBrush="{DynamicResource {x:Static styles:VsBrushes.CommandBarBorderKey}}"
                                        BorderThickness="0,1,0,0"
                                        Padding="8">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="Visible" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsToolSelected}" Value="False">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0"
                                                   Text="{Binding SelectedTool.Name, FallbackValue=''}"
                                                   FontWeight="Bold"
                                                   Margin="0,0,0,4" />

                                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="220">
                                            <ItemsControl ItemsSource="{Binding SelectedTool.Parameters}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Margin="0,4">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="160" />
                                                                <ColumnDefinition Width="*" />
                                                            </Grid.ColumnDefinitions>
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="Auto" />
                                                                <RowDefinition Height="Auto" />
                                                            </Grid.RowDefinitions>

                                                            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
                                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                                                <TextBlock Text="*" Foreground="Red" Margin="2,0,0,0">
                                                                    <TextBlock.Style>
                                                                        <Style TargetType="TextBlock">
                                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding Required}" Value="True">
                                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </TextBlock.Style>
                                                                </TextBlock>
                                                            </StackPanel>

                                                            <TextBlock Grid.Row="1"
                                                                       Grid.Column="0"
                                                                       Text="{Binding Description, FallbackValue=''}"
                                                                       TextWrapping="Wrap"
                                                                       Foreground="{DynamicResource {x:Static styles:VsBrushes.GrayTextKey}}"
                                                                       FontSize="10"
                                                                       Margin="0,0,4,0" />

                                                            <ComboBox Grid.Row="0"
                                                                      Grid.RowSpan="2"
                                                                      Grid.Column="1"
                                                                      IsEditable="True"
                                                                      ItemsSource="{Binding EnumValues}"
                                                                      Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                      VerticalAlignment="Center"
                                                                      Margin="4,0,0,0" />
                                                        </Grid>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>

                                        <Button Grid.Row="2"
                                                Content="Call tool"
                                                Command="{Binding CallSelectedToolCommand}"
                                                HorizontalAlignment="Left"
                                                Margin="0,8,0,0"
                                                Padding="10,3" />
                                    </Grid>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>

                    <!-- Log -->
                    <Border Grid.Row="2" BorderBrush="{DynamicResource {x:Static styles:VsBrushes.CommandBarBorderKey}}" BorderThickness="0,1,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Label Grid.Row="0" Content="Log" FontWeight="Bold" />
                            <TextBox Grid.Row="1"
                                     Text="{Binding RawLog}"
                                     IsReadOnly="True"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto"
                                     HorizontalScrollBarVisibility="Auto"
                                     FontFamily="Consolas"
                                     FontSize="11"
                                     Margin="4" />
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="Settings">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="HTTP headers" FontWeight="Bold" Margin="0,0,0,4" />
                    <TextBlock Grid.Row="1" Text="Headers are sent with every HTTP/SSE request when the HTTP transport is selected." Margin="0,0,0,4" />

                    <Grid Grid.Row="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Name" FontWeight="SemiBold" Margin="4,0" />
                            <TextBlock Grid.Column="1" Text="Value" FontWeight="SemiBold" Margin="4,0" />
                        </Grid>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding Headers}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,2">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <TextBox Grid.Column="0" Text="{Binding Name}" Margin="0,0,2,0" />
                                            <TextBox Grid.Column="1" Text="{Binding Value}" Margin="2,0" />
                                            <Button Grid.Column="2" Content="Remove" Command="{Binding RemoveCommand}" Margin="2,0,0,0" Padding="6,2" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>

                    <Button Grid.Row="3" Content="Add header" Command="{Binding AddHeaderCommand}" HorizontalAlignment="Left" Margin="0,4,0,0" Padding="6,2" />
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</DataTemplate>
